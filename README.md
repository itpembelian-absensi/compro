# Compro

Company profile static site served with PM2 + `npx serve`.

## Deployment

Push ke branch `main` akan otomatis deploy via GitHub Actions self-hosted runner.

### Manual Deploy

```bash
./deploy.sh
```
